import Carousel from "../../Carousel/Carousel";

const Homepage = () => {
    return (
        <div>
            <h1>Homepage</h1>
            <Carousel></Carousel>
        </div>
    );
};

export default Homepage;